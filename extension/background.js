// Makes clicking the toolbar icon open the side panel directly, instead of
// requiring the user to find it via the browser's side panel menu.
chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true });
